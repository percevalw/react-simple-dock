// noinspection ES6UnusedImports
import './globals';
import "./lib/index.css";

const plugin = {
    id: "pret_simple_dock:plugin", // app
    activate: () => null,
    autoStart: true,
};

export default plugin;
